#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char* read_input(size_t initial_size)
    {
        char *buffer = (char *)malloc(initial_size * sizeof(char));
        size_t buffsize = initial_size;
        size_t input_length =0;

        
    }
}