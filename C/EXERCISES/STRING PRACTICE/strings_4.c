#include <stdio.h>

// create an array without room for the null charachter and see how it prints

int main() {
   char my_first_program[11] = "Hello World";
   printf(my_first_program);
   return 0;
}